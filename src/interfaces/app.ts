export enum Lang {
    IT = "it",
    EN = "en",
    FR = "fr",
    ES = "es"
}
export enum BooleanValues {
    True = "true",
    False = "false"
}
export enum ValidationAction {
    Reject = "reject",
    Confirm = "confirm"
}
